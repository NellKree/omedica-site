# ОМЕДИКА site

React/Vite website for a GitHub Pages-ready medical center website.

```bash
npm install
npm run dev
npm run typecheck
npm run build
```

Routing uses hash URLs (`#/services`, `#/doctors`, `#/appointment`) so the built site works on GitHub Pages without server fallback configuration.
